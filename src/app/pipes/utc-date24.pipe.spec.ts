import { UtcDate24Pipe } from './utc-date24.pipe';

describe('UtcDate24Pipe', () => {
  it('create an instance', () => {
    const pipe = new UtcDate24Pipe();
    expect(pipe).toBeTruthy();
  });
});

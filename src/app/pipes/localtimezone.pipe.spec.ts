import { LocaltimezonePipe } from './localtimezone.pipe';

describe('LocaltimezonePipe', () => {
  it('create an instance', () => {
    const pipe = new LocaltimezonePipe();
    expect(pipe).toBeTruthy();
  });
});
